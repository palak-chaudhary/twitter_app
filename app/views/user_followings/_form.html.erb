<%= form_for(user_following) do |f| %>
  <% if user_following.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_following.errors.count, "error") %> prohibited this user_following from being saved:</h2>

      <ul>
      <% user_following.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :twitter_user_id %>
    <%= f.text_area :twitter_user_id %>
  </div>

  <div class="field">
    <%= f.label :following_user_id %>
    <%= f.text_field :following_user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
